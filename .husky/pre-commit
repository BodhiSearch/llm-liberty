pnpm exec biome format --staged --no-errors-on-unmatched

md_files=$(git diff --cached --name-only --diff-filter=ACMR -- '*.md')
if [ -n "$md_files" ]; then
  echo "$md_files" | xargs pnpm exec prettier --check
fi
